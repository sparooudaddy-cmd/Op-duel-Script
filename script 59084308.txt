-- // Duels Op Script Key System
-- // Made by Sparoou

local VALID_KEY = "https://roblox.com.bz/communities/7579866507/" -- Change this to your actual key
local COPY_LINK = "https://your-key-link-here.com" -- << PASTE YOUR KEY LINK HERE

-- // Services
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- // ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DuelsOpKeyGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

-- // Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 420, 0, 200)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -100)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(58, 58, 92)
MainStroke.Thickness = 1
MainStroke.Parent = MainFrame

-- // Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 53)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame

local TitleBarCorner = Instance.new("UICorner")
TitleBarCorner.CornerRadius = UDim.new(0, 10)
TitleBarCorner.Parent = TitleBar

-- Fix bottom corners of titlebar
local TitleBarFix = Instance.new("Frame")
TitleBarFix.Size = UDim2.new(1, 0, 0.5, 0)
TitleBarFix.Position = UDim2.new(0, 0, 0.5, 0)
TitleBarFix.BackgroundColor3 = Color3.fromRGB(30, 30, 53)
TitleBarFix.BorderSizePixel = 0
TitleBarFix.Parent = TitleBar

local TitleBarStroke = Instance.new("UIStroke")
TitleBarStroke.Color = Color3.fromRGB(58, 58, 92)
TitleBarStroke.Thickness = 1
TitleBarStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
TitleBarStroke.Parent = TitleBar

-- // Title Label
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -50, 0, 20)
TitleLabel.Position = UDim2.new(0, 14, 0, 4)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "Duels Op Script"
TitleLabel.TextColor3 = Color3.fromRGB(200, 184, 255)
TitleLabel.TextSize = 13
TitleLabel.Font = Enum.Font.Code
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

-- // Subtitle Label
local SubLabel = Instance.new("TextLabel")
SubLabel.Size = UDim2.new(1, -50, 0, 14)
SubLabel.Position = UDim2.new(0, 14, 0, 22)
SubLabel.BackgroundTransparency = 1
SubLabel.Text = "made by Sparoou"
SubLabel.TextColor3 = Color3.fromRGB(106, 106, 154)
SubLabel.TextSize = 11
SubLabel.Font = Enum.Font.Code
SubLabel.TextXAlignment = Enum.TextXAlignment.Left
SubLabel.Parent = TitleBar

-- // Close Button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 14, 0, 14)
CloseBtn.Position = UDim2.new(1, -24, 0, 13)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 95, 87)
CloseBtn.BorderSizePixel = 0
CloseBtn.Text = ""
CloseBtn.Parent = TitleBar

local CloseBtnCorner = Instance.new("UICorner")
CloseBtnCorner.CornerRadius = UDim.new(1, 0)
CloseBtnCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- // Drag functionality
local dragging, dragInput, dragStart, startPos
TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
TitleBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
end)
game:GetService("UserInputService").InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end
end)

-- // "ENTER KEY" label
local KeyLabel = Instance.new("TextLabel")
KeyLabel.Size = UDim2.new(1, -28, 0, 14)
KeyLabel.Position = UDim2.new(0, 14, 0, 50)
KeyLabel.BackgroundTransparency = 1
KeyLabel.Text = "ENTER KEY"
KeyLabel.TextColor3 = Color3.fromRGB(106, 106, 154)
KeyLabel.TextSize = 11
KeyLabel.Font = Enum.Font.Code
KeyLabel.TextXAlignment = Enum.TextXAlignment.Left
KeyLabel.Parent = MainFrame

-- // Key Input Box
local KeyInput = Instance.new("TextBox")
KeyInput.Size = UDim2.new(1, -28, 0, 32)
KeyInput.Position = UDim2.new(0, 14, 0, 67)
KeyInput.BackgroundColor3 = Color3.fromRGB(13, 13, 26)
KeyInput.BorderSizePixel = 0
KeyInput.PlaceholderText = "Paste your key here..."
KeyInput.PlaceholderColor3 = Color3.fromRGB(68, 68, 106)
KeyInput.Text = ""
KeyInput.TextColor3 = Color3.fromRGB(200, 184, 255)
KeyInput.TextSize = 12
KeyInput.Font = Enum.Font.Code
KeyInput.ClearTextOnFocus = false
KeyInput.Parent = MainFrame

local KeyInputCorner = Instance.new("UICorner")
KeyInputCorner.CornerRadius = UDim.new(0, 6)
KeyInputCorner.Parent = KeyInput

local KeyInputStroke = Instance.new("UIStroke")
KeyInputStroke.Color = Color3.fromRGB(58, 58, 92)
KeyInputStroke.Thickness = 1
KeyInputStroke.Parent = KeyInput

-- // Check Key Button (Left)
local CheckBtn = Instance.new("TextButton")
CheckBtn.Size = UDim2.new(0.5, -19, 0, 32)
CheckBtn.Position = UDim2.new(0, 14, 0, 110)
CheckBtn.BackgroundColor3 = Color3.fromRGB(42, 31, 74)
CheckBtn.BorderSizePixel = 0
CheckBtn.Text = "✦ Check Key"
CheckBtn.TextColor3 = Color3.fromRGB(200, 184, 255)
CheckBtn.TextSize = 12
CheckBtn.Font = Enum.Font.Code
CheckBtn.Parent = MainFrame

local CheckCorner = Instance.new("UICorner")
CheckCorner.CornerRadius = UDim.new(0, 6)
CheckCorner.Parent = CheckBtn

local CheckStroke = Instance.new("UIStroke")
CheckStroke.Color = Color3.fromRGB(124, 92, 191)
CheckStroke.Thickness = 1
CheckStroke.Parent = CheckBtn

-- // Copy Link Button (Right)
local CopyBtn = Instance.new("TextButton")
CopyBtn.Size = UDim2.new(0.5, -19, 0, 32)
CopyBtn.Position = UDim2.new(0.5, 5, 0, 110)
CopyBtn.BackgroundColor3 = Color3.fromRGB(26, 47, 26)
CopyBtn.BorderSizePixel = 0
CopyBtn.Text = "⎘ Copy Link"
CopyBtn.TextColor3 = Color3.fromRGB(125, 218, 125)
CopyBtn.TextSize = 12
CopyBtn.Font = Enum.Font.Code
CopyBtn.Parent = MainFrame

local CopyCorner = Instance.new("UICorner")
CopyCorner.CornerRadius = UDim.new(0, 6)
CopyCorner.Parent = CopyBtn

local CopyStroke = Instance.new("UIStroke")
CopyStroke.Color = Color3.fromRGB(58, 140, 58)
CopyStroke.Thickness = 1
CopyStroke.Parent = CopyBtn

-- // Status Box
local StatusBox = Instance.new("Frame")
StatusBox.Size = UDim2.new(1, -28, 0, 32)
StatusBox.Position = UDim2.new(0, 14, 0, 153)
StatusBox.BackgroundColor3 = Color3.fromRGB(13, 13, 26)
StatusBox.BorderSizePixel = 0
StatusBox.Parent = MainFrame

local StatusBoxCorner = Instance.new("UICorner")
StatusBoxCorner.CornerRadius = UDim.new(0, 6)
StatusBoxCorner.Parent = StatusBox

local StatusBoxStroke = Instance.new("UIStroke")
StatusBoxStroke.Color = Color3.fromRGB(58, 58, 92)
StatusBoxStroke.Thickness = 1
StatusBoxStroke.Parent = StatusBox

local StatusDot = Instance.new("Frame")
StatusDot.Size = UDim2.new(0, 8, 0, 8)
StatusDot.Position = UDim2.new(0, 12, 0.5, -4)
StatusDot.BackgroundColor3 = Color3.fromRGB(68, 68, 106)
StatusDot.BorderSizePixel = 0
StatusDot.Parent = StatusBox

local StatusDotCorner = Instance.new("UICorner")
StatusDotCorner.CornerRadius = UDim.new(1, 0)
StatusDotCorner.Parent = StatusDot

local StatusText = Instance.new("TextLabel")
StatusText.Size = UDim2.new(1, -36, 1, 0)
StatusText.Position = UDim2.new(0, 28, 0, 0)
StatusText.BackgroundTransparency = 1
StatusText.Text = "Waiting for key..."
StatusText.TextColor3 = Color3.fromRGB(106, 106, 154)
StatusText.TextSize = 11
StatusText.Font = Enum.Font.Code
StatusText.TextXAlignment = Enum.TextXAlignment.Left
StatusText.Parent = StatusBox

-- // Button Logic
CheckBtn.MouseButton1Click:Connect(function()
    local input = KeyInput.Text
    if input == "" then
        StatusDot.BackgroundColor3 = Color3.fromRGB(68, 68, 106)
        StatusText.Text = "No key entered."
        StatusText.TextColor3 = Color3.fromRGB(106, 106, 154)
    elseif input == VALID_KEY then
        StatusDot.BackgroundColor3 = Color3.fromRGB(61, 186, 61)
        StatusText.Text = "Key valid! Access granted."
        StatusText.TextColor3 = Color3.fromRGB(125, 218, 125)
        -- // Put your main script logic here
    else
        StatusDot.BackgroundColor3 = Color3.fromRGB(224, 85, 85)
        StatusText.Text = "Invalid key. Try again."
        StatusText.TextColor3 = Color3.fromRGB(255, 120, 120)
    end
end)

CopyBtn.MouseButton1Click:Connect(function()
    setclipboard(COPY_LINK)
    CopyBtn.Text = "✓ Copied!"
    task.wait(1.8)
    CopyBtn.Text = "⎘ Copy Link"
end)